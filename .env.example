# Reel MCP — environment. Copy to `.env` and fill in.

# Your OpenAI API key (the GPT key). Used for: Sora-2 video, TTS, Whisper, gpt-image-1.
OPENAI_API_KEY=

# Optional: override the API base (e.g. for Azure OpenAI or a proxy).
# OPENAI_BASE_URL=https://api.openai.com/v1
